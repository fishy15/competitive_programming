#include <iostream>
#include <fstream>
#include <vector>

#define MAX 40001

using namespace std;

int main() {
    ifstream fin("reduce.in");
    ofstream fout("reduce.out");

    int maxX[3] = {0, 0, 0};
    int maxY[3] = {0, 0, 0};
    int minX[3] = {0, 0, 0};
    int minY[3] = {0, 0, 0};

    int N; fin >> N;
    
}